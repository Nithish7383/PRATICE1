✅ Project Setup & Installation Guide

Release Risk Intelligence – Backend + LLM (Ollama)

This document lists ALL requirements and exact steps to run the project on a fresh machine.

1️⃣ SYSTEM REQUIREMENTS
Operating System

Windows 10 / 11 (64-bit)

macOS / Linux (Ollama supported)

Hardware (Minimum)

RAM: 8 GB (16 GB recommended)

Disk: 10 GB free space

CPU: Any modern 64-bit processor

⚠️ GPU is NOT mandatory (CPU works fine for Mistral).

2️⃣ SOFTWARE REQUIREMENTS
🔹 1. Python

Python 3.10 – 3.13

Verify:

python --version


If not installed → install from:
👉 https://www.python.org/downloads/

✔️ During install, check “Add Python to PATH”

🔹 2. Ollama (LLM Runtime)

Ollama runs the LLM locally.

Download & install:
👉 https://ollama.com/download

Verify installation:

ollama --version


Start Ollama service (auto-starts on Windows after install):

ollama list

🔹 3. Download Required LLM Model

Run once:

ollama pull mistral


Verify:

ollama list


Expected output:

mistral:latest

3️⃣ PROJECT SETUP
🔹 1. Clone / Copy Project
backend_truth_engine_FIXED/
│
├── app/
│   ├── main.py
│   ├── ai/
│   │   ├── ollama_llm.py
│   │   ├── release_risk_ai.py
│   │   └── prompt_template.txt
│   ├── service/
│   ├── ingestion/
│   ├── data/
│   │   ├── test_results.csv
│   │   ├── commit_activity.csv
│   │   ├── defects.csv
│   │   └── pipeline_runs.csv
│
├── venv/
└── requirements.txt

4️⃣ PYTHON VIRTUAL ENVIRONMENT (RECOMMENDED)
🔹 Create venv
python -m venv venv

🔹 Activate venv
Windows (PowerShell)
venv\Scripts\activate


⚠️ If blocked by execution policy:

Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned


Then activate again.

macOS / Linux
source venv/bin/activate

5️⃣ PYTHON DEPENDENCIES
🔹 requirements.txt (MANDATORY)

Create this file:

pandas
pydantic
requests
ollama

🔹 Install dependencies
pip install -r requirements.txt


Verify Ollama python client:

python -c "import ollama; print('ollama OK')"

6️⃣ OLLAMA PYTHON INTEGRATION (IMPORTANT)
app/ai/ollama_llm.py (FINAL VERSION)
import ollama

class OllamaLLM:
    def __init__(self, model="mistral"):
        self.model = model

    def generate(self, prompt: str) -> str:
        response = ollama.chat(
            model=self.model,
            messages=[
                {"role": "user", "content": prompt}
            ],
            options={
                "temperature": 0.3,
                "num_predict": 400
            }
        )
        return response["message"]["content"]


✔️ ollama must be running in background

7️⃣ RUN THE PROJECT

From project root:

python app/main.py


Expected output:

===== AI EXPLANATION =====
Explanation:
...
Key Risk Drivers:
...
Recommended Actions:
...

8️⃣ COMMON ERRORS & FIXES
❌ ModuleNotFoundError: ollama

✔ Fix:

pip install ollama

❌ ollama.chat: connection refused

✔ Fix:

ollama serve


(or restart Ollama app)

❌ release_id mismatch

✔ Fix:
Ensure ALL CSV files contain the SAME release_id

BILLING_SERVICE_REL_2025_Q4

9️⃣ WHY THIS SETUP IS ENTERPRISE-READY

✔ Runs fully offline
✔ No API keys
✔ No production data exposure
✔ Deterministic outputs
✔ Auditable prompt + metrics
✔ Easy to deploy on any laptop

10️⃣ ONE-LINE SUMMARY (FOR DOCUMENTATION)

This project uses Python, rule-based SDLC metrics, and a locally hosted Mistral LLM via Ollama to generate explainable release risk insights without exposing production data or relying on external APIs.